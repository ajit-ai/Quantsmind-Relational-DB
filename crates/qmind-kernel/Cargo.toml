[package]
name = "qmind-kernel"
description = "QuantsMind storage kernel: pages, buffer pool, B+Tree, WAL, MVCC"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["cargo_bench_support"] }

[[bench]]
name = "kernel_bench"
harness = false
