Concepts
========

Conceptual guides that explain how the engine is organized, read ``outside-in``
(what the user sees down to the bytes on disk) and ``inside-out`` (how the
durability and recovery machinery works).

.. toctree::
   :maxdepth: 2
   :caption: Core concepts

   how-it-works
   storage-model
   execution-model
   mvcc
   transactions
   concurrency
   locking
   deadlocks